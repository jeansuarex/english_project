{
  "name": "shakespeare-project",
  "private": true,
  "scripts": {
    "start": "concurrently -n api,web,admin -c blue,green,yellow \"bun run --cwd api src/index.ts\" \"bun run --cwd web/app dev\" \"bun run --cwd admin/app dev\"",
    "start:api": "bun run --cwd api src/index.ts",
    "start:web": "bun run --cwd web/app dev",
    "start:admin": "bun run --cwd admin/app dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
